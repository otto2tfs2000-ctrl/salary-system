# Otto2 系統開發交接（2026-08-18）

給接手的人：這份取代所有更早的版本。08-09～08-11 的紀錄還留在後面（架構、踩過的坑都還有效），
但版本號、待辦清單這些「現況」都已經被新加的「零之一、08-13～08-18 又做了這些事」蓋過去了，
**衝突的地方一律以最前面的區塊為準**。

---

## 零之一、08-13～08-18 又做了這些事（現行版本）

### 現在的版本號（改任何一支之前先確認手上這份對不對）
- `otto2-notify/server.js`：`2026-08-18-mybookings`
- `otto2artclub-booking/index.html`：`v4.24`
- `salary-system/booking.js`：`20260818b`
- `salary-system/inventory.js`：`20260815b`
- `salary-system/consumables.js`：`20260817b`
- `salary-system/app.js`、`finance.js`、`member.js`：版本號散在各自的 commit，改之前直接 `grep` `index.html` 裡對應的 `?v=`，不要用這份文件記的數字，這份不保證每次都同步更新。

### 新增功能
- **座位表**（`booking.js` `SEAT_AREAS`）：今日排課每個時段自動建議客人坐吧台／畫架／教室／臨時桌，5-7歲選圖小朋友優先吧台。**是「點兩下」不是拖曳**——拖曳在老闆手機上測了兩輪都失敗（Pointer Events 相容性問題），改成點卡片再點目標區塊。手動指定過的位置存在 `booking.seatArea`，不會被自動建議洗掉。「壩台」已經正名成「吧台」（含歷史資料一起改過，不會有殘留的舊名字對不起來）。
- **AI 小幫手**（`/liff/assistant`，客人端右下角浮標）：跟 LINE 官方帳號的客服機器人是兩套完全獨立的東西（那支不一定隨時開著，這支嵌在網頁上）。用聊天收集人數/課程/日期，湊齊後跳去精靈第 3 步讓客人自己完成最後確認，AI 不會替客人按送出。系統提示詞裡限制了：不主動報價、有真實班表感知（不會建議公休日）、體驗價/會員價會收斂成一個「非會員價」講給 AI，避免它亂報優惠。
- **上午／下午分開排老師**（`bkSchedVal` 的 `tPM` 欄位）：以前白天三個時段（10-12、14-16、16-18）共用一個老師數，現在 10-12 算上午、14-16+16-18 算下午，各自獨立設定+算容量。**這個容量公式在三個地方各自重複實作**（`booking.js`、`server.js` 的 `/liff/availability`、客人端 `index.html` 的 `capOfSlot`），三邊都已經同步改過，**以後只要動容量公式，三個檔案要一起改，漏一個客人端看到的名額就會跟後台對不起來**。
- **即時通知系統**（`booking.js` 的 `bkStartLiveWatch`/`bkHandleSSE`）：今日排課分頁會跟 Firebase 開一條 SSE 連線（`EventSource`，帶 `Accept:text/event-stream` 的 GET，Firebase REST 原生支援，不用另外裝 SDK），客人自己用 LINE 送出新預約時：分頁旁邊紅點會即時+1、卡片標「NEW」、（授權過的話）跳瀏覽器桌面通知。客人自己取消預約時也會跳通知（見下面「客人自助取消」）。這個機制完全不牽涉 LINE 個人／群組推播，是老闆明確要求「不要跟私人帳號有關係」之後做的。
- **客人自助查詢／取消預約**（新端點 `/liff/mybookings`、`/liff/cancelBooking`）：客人端「查詢我的點數」頁面下方多一區「我的預約」，列出還沒核銷、還沒取消的預約，可以自己按取消（當天的不給線上取消，要私訊小編；已核銷的也不給取消）。**沒有做「改時段」的獨立介面**——改時段的流程是引導客人先取消、再用「我要預約課程」重新走一次完整精靈，故意不重寫一套名額檢查邏輯。取消時會在 booking 上標 `cancelledBy:"customer"`，後台的即時通知系統就是靠這個欄位分辨「這是客人自己做的」。
- **收訂金末五碼**：選 LINE Pay／匯款時會多一個「末五碼」欄位方便對帳，現金/刷卡/文化幣不會出現這欄。
- **今日排課核銷旁邊加「賣方案」捷徑**、**客人姓名可以點開直接看點數/堂數/紅利**，不用切去會員分頁。
- **耗材記帳**：「只顯示未撥款」篩選＋匯出會跟著篩選走，方便行政抓「還沒處理的那批」單獨匯出報帳。

### 修正的 bug
- **`修正核銷` 的餘額預覽會誤顯示成又扣一次**：改同一個付款人時，沒有先把舊扣款加回去再算「剩多少」，導致畫面顯示嚇人的錯誤數字（實際入帳沒錯，只是預覽畫面誤導人）。已修正。
- **座位表人數算法**：曾經不管幾位、每筆預約都當成佔 1 個位子算，兩位的預約看起來只佔一張椅子。已改成照 `people`/`party` 算實際人數。
- **修改預約時段時名額檢查把自己算兩次**：`bkSlotInfo` 原本沒有排除「正在編輯的這一筆」，導致改預約明明沒加人，系統卻說「會變成更多位、超過表定上限」。已加 `excludeId` 參數修正。
- **改時段沒有同步清掉 `actualTime`**：客人自己在網頁上約的預約會存一個 `actualTime`（跟 `slot`分開，處理提早到館用），行政後台改時段時漏了同步清掉，導致「預約成功通知」跟「前一天上課提醒」顯示不同時間，客人搞不清楚哪個才對。已修正：改日期或時段時自動清掉舊的 `actualTime`，且「通知客人」勾選框會自動幫忙勾起來（以前編輯預約預設不勾，容易忘記通知客人真正改了時間）。
- **`/liff/me`（客人端認人用的端點）沒有 fallback**：只查 `liffProfiles`/`lineIndex` 這兩個反向索引，但很多會員只有「正向」的 `members/{phone}/lineUserId`，反向索引沒補上，導致同一個 LINE 帳號每次都要重新輸入電話。已加上反查 `members` 的 fallback，順便自我修復反向索引。
- **選圖繪畫／創作繪畫 缺會員價/單次原價的材料配方**：只有體驗價那個規格有設定用料，導致大部分核銷都沒扣到庫存。已補上四筆配方資料。
- **加購畫布升級沒有排除底層規格自帶的畫布**：客人加購大尺寸畫布時，原本規格內附的小畫布材料還是會被扣，變成扣兩次。已加 `skipCats` 機制，加購跟同一分類的規格內附材料互斥。

### 環境/踩坑筆記（給接手的人）
- **Browser 測試工具偶爾會出現 `window.innerHeight===0`** 的假象（分頁沒真的 attach），導致座標點擊、`elementFromPoint` 之類的操作靜默失敗、卻不報錯。呼叫一次 `computer{action:"screenshot"}` 通常能讓分頁重新 attach。這是測試環境的問題，不是應用程式本身的 bug，遇到「明明程式碼看起來對，但測試結果是空的」要先懷疑這個。
- **這個環境沒有 Node.js**，`booking.js`/`inventory.js` 這些都用 IIFE 包起來，內部函式不會掛在 `window` 上。要驗證邏輯，是把整支原始檔複製一份，用瀏覽器載入、mock 掉 `fetch`/`localStorage`/`Notification`/`EventSource` 等等，跑出真實的 DOM 結果來看——不是憑空猜邏輯對不對。
- **`salary-system` 資料夾裡混進一份過期的 `server.js`**（38K，Aug 10 的舊版，938 行；正式的在 `otto2-notify` 裡，1700+ 行）。這份沒有被任何地方部署或引用，是無害的殘留檔案，但改東西之前**千萬不要誤改到這份**，先確認路徑是 `otto2-notify-main/server.js`。

---

## 一、系統長什麼樣

三個 GitHub 倉庫，一個 Railway 服務，資料在 Firebase 和 Google 試算表。

| 倉庫 | 用途 | 上線位置 |
|---|---|---|
| `salary-system` | **後台**（老師與行政用）。index.html 只是外殼，功能都在那十支 .js | GitHub Pages |
| `otto2artclub-booking` | **客人端預約頁**。整支程式都在 `index.html` 裡 | GitHub Pages |
| `otto2-notify` | **Railway 後端**。LINE 推播、行事曆資料、客人端讀取端點 | Railway 自動部署 |

**Railway 服務網址**
`https://otto2-notify-production.up.railway.app`
改完 server.js 一定要打 `/health` 確認 `version` 變成新的，才算真的部署上去。

**Firebase**
- `otto2-booking-f9ef7`：members（會員、明細、票券）、bookings（預約）、schedule（班表）、lineIndex、liffProfiles
  - `https://otto2-booking-f9ef7-default-rtdb.asia-southeast1.firebasedatabase.app`
- `otto2-2026`：salaryData（薪資、業績、方案設定、庫存、用料）、deductions

**Google 試算表**（課程、班表、方案、加購都在這裡，老闆自己維護）
ID `1QjiDwmPcwbmdhmNv9cz1A6veC_BbC75m1VJG85P3Q6M`

分頁與欄位：
- **課程**：A 分類／B 課程名稱／C 說明／D 規格／E 時長／F 價格／G 圖片網址／H 上架／I 排序／J 最小年齡／K 可加堂／L 可加購／**M 佔位**／**N 計時**
  - 前 11 欄照位置讀（不可插欄），M、N 之後的新欄位照標題找
  - 佔位：填 3 代表一組佔 3 個名額（地毯課）
  - 計時：填 700 代表每小時 700，客人自選 2–4 小時
- **加購**：課程名稱／加購名稱／價格／排序。客人端會自動顯示可複選的加購
- **舊方案單價**（可能還沒建）：票券名稱／總價／堂數／效期月數／生效日起／單堂價／備註

**LIFF**
LIFF ID `2010906803-FMDYktUN`，網址 `https://liff.line.me/2010906803-FMDYktUN`
LINE 圖文選單已經指向這個網址（08-09 修正）。

---

## 二、踩過的坑（最重要，請先看這節）

### 1. 改錯檔案，三輪工作白做
`otto2artclub-booking` 裡有 `index.html` 和 `liff-index.html` 兩支。
**GitHub Pages 服務的是 `index.html`**，但連續三輪都在改 `liff-index.html`，
客人端從頭到尾沒生效，中間還測了好幾次都以為是別的問題。

- `liff-index.html` 已經沒用了，**建議直接刪掉**（`salary-system` 裡也有一份舊的，一起刪）
- 動客人端之前，先確認 GitHub Pages 到底服務哪一支

### 2. 版本號沒跳，等於沒上線
`salary-system/index.html` 裡每支 .js 後面都有 `?v=日期`。
**改了 .js 就一定要跳版本號**，不然瀏覽器讀快取的舊檔。
曾經整晚四支檔案的版本號都沒跳，開發者硬重整看得到新功能，
其他同事開後台讀到的全是舊版。

### 3. 拿舊檔改，差點洗掉別人的工作
曾經拿使用者手上的舊 `server.js` 疊修改，
差點把當天早上做好的 `/staff/list` 整段洗掉。
**動任何檔案前，先確認手上這份是不是線上正在跑的那份。**
server.js 可以用 `/health` 的 `version` 比對。

### 4. gviz 的標題列不在資料裡
`liff-index.html` 用 `j.table.rows`，Google **已經把標題列抽到 `j.table.cols`**，
所以 `rows[0]` 是第一筆資料不是標題。
`booking.js` 則是自己 `rows.shift()` 砍標題。兩支行為不同，照抄會出錯。

### 5. 客人端有 localStorage 快取
`SHEET_CACHE_KEY`（目前 `otto2_liff_sheets_v3`）。
改了解析邏輯要把這個鍵往下跳，否則所有客人讀的還是舊格式的暫存。

---

## 三、這三天完成的事

### 後端 `server.js`（現行版本 `2026-08-10-multislot`）
`/health` 應該回傳這五個旗標，缺一個就代表部署有問題：
`hasStaffList` `hasLiffRead` `hasCalendarFeed` `hasSeats`（外加 version）

新增的端點：
- `/notify/plan` — 賣方案後推 LINE 卡片（方案內容、點數拆三行、到期日、目前餘額）
- `/liff/me`、`/liff/slots`、`/liff/member`、`/liff/ledger` — **客人端改走這裡，不再直讀 Firebase**
- `/cron/bookings` — 給 Google Apps Script 同步行事曆用（CRON_KEY 保護）

`/liff/slots` 的名額計算已支援：`seats`（佔位）、`slots[]`（多時段）、`slot2`（連堂）、跳過 cancelled/expired。

### 客人端 `otto2artclub-booking/index.html`（v4.8a）
- 五處直讀 Firebase 全部改走 Railway（原本 `/bookings.json` 公開可讀，含所有客人姓名電話）
- 晚上時段 18:30–21:00（班表有排才出現）
- 佔位（地毯一組佔 3 位）與計時課（每小時 700、2–4 小時）
- 連堂不跨越白天／晚上的斷點

### 後台 `salary-system`
**booking.js（`20260810i`）**
- 班表可逐日加開晚上時段，晚上老師數獨立算容量
- 手動登記時段**可複選**（畫一整天選三格），存 `slots[]`，同時保留 `slot`/`slot2` 相容
- 課程下拉依分類分組，顯示價格
- 核銷可以改課程項目、可以改扣堂數
- **修了會賠錢的 bug**：扣堂數原本寫死 1，兩個人堂數扣抵只扣一堂、只認列一堂營收
- 老師可複選（`teachers[]` 陣列，同時存頓號字串相容）
- 舊方案單價改從試算表讀，第三順位用堂數比對「方案設定」
- 日期標題可點開整月月曆，每天顯示幾組幾位

**member.js（`20260809h`）** — 明細單筆可編輯、來源標記、票券匯入、賣方案 LINE 通知、新舊客手動切換
**app.js（`20260810a`）** — 每日登記認得多老師，人次按老師數平分
**inventory.js（`20260809a`）** — 品項多了「售價」欄（跟成本分開）
**recipe.js（`20260811a`）** — 材料清單自然排序（2F 排在 10F 前面），分類順序跟著庫存盤點的拖曳順序

### Google Apps Script（專案名「Otto2 上課提醒」）
兩支函式，都設了觸發器：
- `remind()` — 每天早上 9–10 點推前一日上課提醒
- `syncCalendar()` — 每小時同步預約到「Otto2 預約」行事曆（活動說明裡的 `[otto2:預約編號]` 是同步依據，不可刪）

---

## 四、還沒收尾的事（按急迫度）

### 高
1. **「儲存失敗（本機備份）」紅字沒查**
   `salaryData` 沒寫進 Firebase，只留在瀏覽器。會員餘額有進雲端、業績只在本機，
   **兩邊會靜默對不起來**。要在 Console 看實際錯誤訊息（大概是 PERMISSION_DENIED）。

2. **27 筆手機格式壞掉的會員**，身上掛著約四萬儲值金。
   LIFF 是用電話當 key 寫 `lineUserId` 的，格式不一致會寫到別的節點。

3. **鎖 Firebase `.read`**
   客人端已經不直讀了，`bookings` 和 `members` 的 `.read` 可以關掉。
   關之前確認：預約頁能看名額、我的點數頁能看餘額與使用紀錄。
   `schedule` 沒有個資可以留著。

### 中
4. **賣方案沒記業務人員**：`planSales` 缺 `by` 欄位，月報算不出業績歸屬。
   要加「業務」下拉（預設登入者、可改）。
   **卡在一個決策**：會館流水帳系統是把賣方案當月營收，還是逐次認列？
   兩邊都當營收就會重複計算。老闆要跟行政確認。

5. **多人堂數扣抵的舊核銷要補正**：`useSe` 修好之前，
   凡是「付款方式＝堂數扣抵」且「人數 > 1」的核銷都少扣了堂數、少認了營收。
   重新核銷會自動沖銷再寫入。要先查出清單。

6. **32 筆票券沒有單價**、63 筆票券備註還沒匯入。
   單價現在可以自己在「舊方案單價」分頁補，不用改程式。

7. **地毯課的用料**：合併成同一門課之後，加購（例如加背景）的材料不會被扣。
   庫存會慢慢失準。（08-17 已經修好「加購畫布升級不會重複扣底層規格畫布」這個通用機制，
   但地毯課**本身的基礎配方還是沒設定**，這是兩件不同的事，地毯這筆還沒處理。）

8. **庫存幾乎全部沒填單位成本**，所以用料成本、毛利率整片是空的。

9. **幾樣品項完全沒有材料配方**：邏輯素描課程（所有規格都沒設）、黃金雪山擴香流動石、
   流動時鐘/鏡子、流動杯子、大頭娃娃創作(20x20cm)。核銷這些不會扣到任何庫存。
   要等老闆確認每樣實際用什麼材料才能補，之前有整理成表格給他看過，還沒回覆。

10. **未來材料短缺預警**：老闆問過「有沒有辦法提前知道某天某材料會不夠」，
    已經提案「抓未來N天的預約、加總需要的材料、跟現有庫存比對、不夠就提前標紅」，
    老闆說**先不用，再想想**——不是否決，是還沒決定要不要做，之後如果又提到這件事可以直接接著做。

### 低
11. 客人端開頁面有一到三秒空白（gviz 抓課程資料，第一次進來沒快取）
12. 邱宗洲、劉芷蕎的測試點數要歸零（用「調整餘額」補反向，不要刪明細）
13. 八月資料補登

---

## 五、老闆的工作習慣

- **一次做一件事，做完確認再下一件。** 不要一次丟三個改動。
- **要完整檔案，不要 diff。** 他是下載檔案手動上傳 GitHub（現在要改用 Code 直接改）
- 直接、講重點，不要客套。專業術語要翻成白話。
- **他質疑你的時候，如果沒有提出新證據或新邏輯，不要改口。** 說明理由並堅持。
- 講「好了」通常是指上一步做完了，但不一定驗證過——**要主動要求他確認具體的畫面或數字**。
- **不想要客人/員工通知牽涉到任何個人 LINE 帳號或群組**，即使一開始討論方向是往 LINE 群組走，
  問清楚後才發現他要的是「完全不牽涉個人身份」的方式（後來做了純網頁內的紅點/桌面通知）。
  遇到通知類需求，先假設他不想用個人 LINE，除非他自己指定。
- **真機測試結果比合成測試結果更可信。** 座位表拖曳功能在合成測試裡兩輪都通過，
  但實機上兩輪都回報「完全拖不動」——後來直接放棄拖曳、改成點兩下，而不是繼續猜相容性問題。
  他回報「在他手機上不行」時，直接相信、換方向，不要堅持原本的做法去猜哪裡不對。
- 交辦一個功能常常會帶出一個更根本的問題（例如「行事曆沒同步」查到最後是 Google Apps Script
  的觸發器從來沒建立過）。**查到根因要老實講，不要只做表面的那個請求。**

---

## 六、接手後的第一件事

先跑一次現況檢查，不要直接開始改：

1. 開 `https://otto2-notify-production.up.railway.app/health`，確認 `version` 是 `2026-08-18-mybookings`（或更新）
2. 確認 `otto2artclub-booking/index.html` 是不是 `v4.24`（或更新，右上角有版本標記）
3. 確認 `salary-system/index.html` 裡每支 .js 的版本號跟檔案更新時間對得上（見最前面「零之一」列的版本號）
4. 問老闆「儲存失敗」那個紅字還在不在——**這個從 08-11 就沒查過，一直是最高優先但沒人碰過**
5. 確認 Google Apps Script「Otto2 上課提醒」專案裡 `syncCalendar` 觸發器還在（08-18 曾經整個消失過一次，
   查了很久才發現，行事曆同步因此停擺好幾天沒人發現）

第 4 點是目前最該處理的，因為它會讓帳目靜默地對不起來，而且已經放了一個禮拜沒人查。

---

## 七、這份文件取代舊版

這份是 2026-08-18 的最新狀態，**取代先前那份 CLAUDE.md**（包含 08-11 那版）。
如果你讀到的資訊跟最前面「零之一」衝突，以「零之一」為準；後面「二、三、四」是
08-09～08-11 的歷史紀錄，架構背景還有效，但版本號、待辦清單已經過期。
特別是這幾點常被搞錯：

- `otto2artclub-booking/liff-index.html` **不是 index.html 的複本**，
  它停留在舊版且已廢棄，客人端讀的是 `index.html`。建議刪除。
- `otto2-notify/server.js` **有三十幾個端點**，不是三個。
  完整清單以檔案內容為準，不要照 README 判斷。
- 使用者手上的 ZIP 可能不是最新的。動任何檔案前，
  先用 `/health` 的 version 或 GitHub 的更新時間確認。
- `salary-system` 資料夾裡有一份過期的 `server.js`（見「零之一」最後一條），
  不是正式在跑的那份，正式的在 `otto2-notify-main/server.js`。
